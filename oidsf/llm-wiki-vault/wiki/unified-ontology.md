# Unified OIDSF ontology (hub)

Normative document: [`../../spec/UNIFIED_ONTOLOGY.md`](../../spec/UNIFIED_ONTOLOGY.md) (repo root relative to this vault).

**Summary:** One YAML namespace under `spec/schemata/*.yaml`; FtM roots preserved; STIX under `IntelObject`; Arkham prefixes dropped; key merges (`OnlineAccount`, `EmploymentPost`).

Related: [[schemata-yaml]].
